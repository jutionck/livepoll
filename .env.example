# PostgreSQL Connection String (Supabase / Neon / Local PostgreSQL)
DATABASE_URL="postgresql://username:password@hostname:5432/databasename?sslmode=require"

# Vercel Cron Secret (Optional, for automatic cleanup endpoint)
CRON_SECRET="your_vercel_cron_secret"
